apiVersion: v1
kind: Service
metadata:
  name: {{ include "cc-backend.fullname" . }}
  labels:
    {{- include "cc-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cc-backend.selectorLabels" . | nindent 4 }}
